const { ok } = require('assert')

describe('Samples Test', () => {
    it('should pass one', () => {
        ok(true)
    })

    it('should pass two', () => {
        ok(true)
    })

    it('should pass three', () => {
        ok(true)
    })
})